# Server-deployable requirements for microrobot MBRL project
# Installs cleanly on Ubuntu/CUDA servers without ROS2 or hardware dependencies.
#
# Usage (after conda env create -f environment.yml):
#   pip install -r requirements-server.txt
#
# GPU requirements:
#   - NVIDIA driver compatible with CUDA 12.x for JAX
#   - PyTorch will auto-detect CUDA; specify index-url if needed

# Core RL frameworks
stable-baselines3==2.8.0
gymnasium==1.2.3
gym==0.19.0  # legacy envs still use old gym

# DreamerV3 (forked for hybrid action spaces)
# NOTE: if `import dreamerv3` fails on missing configs.yaml, see CLAUDE.md
git+https://github.com/Pippo809/dreamerv3.git@d7cab46

# Deep learning backends
torch==2.0.1  # upgraded from 1.13.1 for CUDA 12 / JAX 0.6 compatibility
torchvision==0.15.2
jax==0.6.2
jax-cuda12-plugin==0.6.2
tensorflow-probability==0.25.0

# Vision & image processing
opencv-python==4.13.0.92
opencv-contrib-python==4.13.0.92
scikit-image==0.25.2
pillow==12.2.0
imageio==2.37.3

# Segment Anything (for mask generation)
git+https://github.com/facebookresearch/segment-anything.git@dca509fe793f601edb92606367a655c15ac00fdf

# Scientific computing
numpy==1.26.4
scipy==1.15.3
matplotlib==3.10.9
pandas==2.3.3

# Utilities
pyyaml==6.0.3
tqdm==4.68.1
cloudpickle==1.6.0

# Monitoring / terminal UI (optional)
curtsies==0.4.3

# Hardware control (not needed for simulation-only server training, but kept for completeness)
# Uncomment if running real-world experiments on the server:
# pyserial==3.5
# pyvisa==1.16.2
# pymmcore==12.5.0.75.0
# pymmcore-plus==0.18.1
